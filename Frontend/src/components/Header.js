import React from 'react';
import '../styles.css';

const Header = () => {
    return (
        <header className="header">
            <h1>AI Based: Proctored Examination Portal</h1>
        </header>
    );
};

export default Header;
